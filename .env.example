# Copy to .env for local development.
# On Render, these are set via render.yaml / the dashboard instead.

DATABASE_URL=postgresql://user:password@localhost:5432/rag
ANTHROPIC_API_KEY=sk-ant-...
VOYAGE_API_KEY=pa-...
INGEST_API_KEY=pick-a-long-random-string

# Optional — see app/config.py for defaults
# OWNER_NAME=Titouan
# CORS_ORIGINS=https://example.com,https://foo.com
# LOG_LEVEL=INFO
